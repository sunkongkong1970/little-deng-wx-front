.page{padding:16px}
.publisher{background:#fff;border-radius:12px;padding:12px;margin-bottom:12px}
.row{display:flex;gap:8px;align-items:center}
.annotation{display:block;margin-top:8px;color:#999;font-size:12px}
.feed{display:flex;flex-direction:column;gap:12px}
.post{background:#fff;border-radius:12px;padding:12px}
.head{display:flex;gap:8px;align-items:center}
.nick{font-weight:600}
.meta{margin-top:8px;color:#999;font-size:12px;display:flex;gap:8px;align-items:center}
.actions-row{display:flex;gap:12px;color:#666;font-size:14px;margin-top:8px}
